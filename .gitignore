.venv
.vscode
.ccache

# Claude Code: track shared config, ignore per-user overrides.
.claude/settings.local.json
.claude/.credentials.json
CLAUDE.local.md

bazel-*

# Local CMake user presets (machine-specific, not for version control)
CMakeUserPresets.json

modelfiles/*


*.exe

# Prebuilt Android demo APKs — distribute via GitHub Releases, not in-repo.
*.apk