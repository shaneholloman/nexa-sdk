name: Geniex Demo
id: geniex_chat_android
include_in_cli: false                       # TODO(manu): confirm during review
license_url: https://github.com/qualcomm/ai-hub-apps/blob/main/LICENSE
license_type: bsd-3-clause
status: published
domain: Generative AI
app_type: 'android'
static_img_filename: 'app_screen.png'
app_repo_relative_path: 'geniex_chat_android'
os:
  name: Android
  # version: TODO(manu): set after minSdk audit; demo currently bumps to API 31
languages: ['Kotlin']                        # add 'Java' if anything stays in Java after compose audit
model_file_path: 'src/main/assets/models'    # TODO(manu): adjust if demo lays out models differently
# TODO(manu): models, runtime (new enum value, e.g. 'geniex'), headline,
# description, precisions, qaihm_version, use_case, related_models,
# private_model_s3_paths
